/**
 * High-order component для отрисовки бе дивов
 *
 * @author dayneko_si
 * @since 23.02.2019
 * @param props передаваемые свойства
 * @returns {*} возвращает готовый объект для рендеринга
 */
const aux = (props) => props.children

export default aux;